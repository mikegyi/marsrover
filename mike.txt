


arrray of commands
iterate

variables 
strings



  describe("Mars rover", function() {
    it("turns left", function() {
        const start  = '0 0 N'
        const result = marsrover(start, 'L')
      expect(result).toBe('0, 0 W');
    });
  });

  describe("Mars rover", function() {
    it("turns right", function() {
        const start  = '0 0 N'
        const result = marsrover(start, 'R')
      expect(result).toBe('0, 0 E');
    });
  });   









Test Input:
5 5
1 2 N
LMLMLMLMM
3 3 E
MMRMMRMRRM

marsrover('1 2 N', 'LMLMLMLMM')
 
Expected Output:
1 3 N
5 1 E


const compass = ['N', 'E', 'S', 'W']

 else if (action === 'R') {
        const index = compass.indexOf(position.heading)
        const newIndex = index + 1
        let newHeading = compass[newIndex]
        if(newHeading === undefined) {
            newHeading = 'N'
        }
        position.heading = newHeading         

    }




